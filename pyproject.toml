[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["B", "D102", "E", "F", "I", "UP"]

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["flows"]
testpaths = ["flows/tests"]